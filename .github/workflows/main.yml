name: Assignment_of_GHA

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - master
      - main
  workflow_dispatch:

jobs:
  building_part:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build project
        run: mvn clean install
        working-directory: hello-spring_demo  # Navigate to the correct directory

  testing_part:
    runs-on: ubuntu-latest
    needs: building_part
    steps:
      - name: Test project
        run: mvn clean test
        working-directory: hello-spring_demo  # Navigate to the correct directory

  artifact_upload:
    runs-on: ubuntu-latest
    needs: testing_part
    steps:
      - uses: actions/checkout@v2

      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: build-artifacts
          path: hello-spring_demo/target/  # Specify the correct path

      - name: Push build artifacts to a new branch
        run: |
          git config user.name "anupams20"
          git config user.email "anupams20@iitk.ac.in"
          git checkout -b artefacts-store
          git add .
          git commit -m "Upload build artifacts"
          git push origin artefacts-store
        working-directory: hello-spring_demo  # Ensure you're in the correct directory
